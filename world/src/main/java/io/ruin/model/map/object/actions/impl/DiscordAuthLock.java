package io.ruin.model.map.object.actions.impl;

public class DiscordAuthLock {

  /*  public static int UNAUTH_MAXXP = 2000000;

    public static void Locker(Player p) {
        if (!p.DiscordAuth && p.getStats().totalXp >= UNAUTH_MAXXP) {
            if (p.getPosition().equals(World.EDGEHOME)) {
                CommandHandler.handle(p, "::auth");
            } else {
                CommandHandler.handle(p, "::auth");
                p.getMovement().teleport(World.EDGEHOME);
            }
            p.sendMessage("You must authenticate with the bot to continue playing!");
            p.startEvent(event -> {
                event.delay(2);
                p.lock();
            });
        }*/

}
